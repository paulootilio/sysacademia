<%= form_with(model: fornecedor, local: true) do |form| %>
  <% if fornecedor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fornecedor.errors.count, "error") %> prohibited this fornecedor from being saved:</h2>

      <ul>
      <% fornecedor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-lg-10 form-group">
  
  <div class="row">
  <div class="col-lg-5 form-group">
    <%= form.label :nome, :class => 'control-label' %>
    <%= form.text_field :nome, id: :fornecedor_nome, :class => 'form-control' %>
  </div>
  </div>

  <div class="row">
  <div class="col-lg-5 form-group">
    <%= form.label :email, :class => 'control-label' %>
    <%= form.text_field :email, id: :fornecedor_email, :class => 'form-control' %>
  </div>
  </div>

  <div class="row">
  <div class="col-lg-2 form-group">
    <%= form.label :cpf, :class => 'control-label' %>
    <%= form.text_field :cpf, id: :fornecedor_cpf, :class => 'form-control' %>
  </div>
  </div>

  <div class="row">
  <div class="col-lg-3 form-group">
    <%= form.label :telefone, :class => 'control-label' %>
    <%= form.text_field :telefone, id: :fornecedor_telefone, :class => 'form-control' %>
  </div>
  </div>

  <div class="row">
  <div class="col-lg-7 form-group">  
      <input type="submit" name="commit" class="btn btn-primary" value="Salvar" data-disable-with="Salvar" />
      <%= link_to 'Voltar', fornecedors_path, :class => "btn btn-primary"%>
  </div>
  </div>

  </div>

<% end %>
